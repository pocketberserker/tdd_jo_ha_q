package xutp

import spock.lang.*
import xutp.KeyValueStore

class KeyValueStoreSpec extends Specification {

    def "‹ó‚ÌKVS‚Ékey‚Ævalue‚ÌƒyƒA‚ðput‚·‚é‚Ækey‚É‘Î‰ž‚·‚évalue‚ªget‚Å‚«‚é" () {

		    given:
		    def kvs = new KeyValueStore()

				when:
				kvs.put(key,value)

				then:
				kvs.get(key) == value

				where:
				key   | value
				'aaa' | 'bbb'
				'ccc' | 'ddd'
		}
}